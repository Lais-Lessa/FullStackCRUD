import styled from "styled-components";
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';

export const StyledIconEdit = styled(FontAwesomeIcon)`

  color: #333;
  cursor: pointer;
  
`;